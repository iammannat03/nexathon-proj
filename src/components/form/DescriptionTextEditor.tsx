import React from "react";
import Home from "../text-editor/quill-editor";
import { Button } from "../ui/button";

type Props = {};

const textEditorActions = ["1", "2", "3"];

const DescriptionTextEditor = (props: Props) => {
  return <Home />;
};

export default DescriptionTextEditor;
